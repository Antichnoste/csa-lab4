# Формат микрокоманды (26 бит)

Это структура слова, которое каждый такт выдается из `Micro-ROM` на шину `CONTROL BUS` для управления стадией исполнения (EX) и конвейером.

### 1. `Latch Ctrl` [Биты 25:24] — Управление защелками конвейера
Определяет, что делают стадии `IF` и `ID`, пока работает микрокод на стадии `EX`.
*   **`0` (00)**: **NORMAL** — Конвейер течет штатно. На следующем такте новые инструкции продвинутся вперед.
*   **`1` (01)**: **STALL** — Заморозка. Сигнал летит к защелкам `IF/ID` и `ID/EX`. Они блокируются и не обновляют данные (используется для первого такта при памяти/стеке).
*   **`2` (10)**: **FLUSH** — Сброс. Принудительно очищает защелки (превращает их в `NOP`), спасая конвейер от выполнения мусорных инструкций после совершенного прыжка.
*   **`3` (11)**: *Зарезервировано.*

### 2. `ALU Op` [Биты 23:21] — Операция АЛУ
Указывает Арифметико-Логическому Устройству, какую операцию выполнить над левым (`ACC`) и правым (`MUX B`) операндами.
*   **`0` (000)**: **PASS B / NOP** — Пропустить правый операнд сквозь АЛУ без изменений.
*   **`1` (001)**: **ADD** — Сложить (`ACC` + `MUX B`).
*   **`2` (010)**: **SUB** — Вычесть (`ACC` - `MUX B`).
*   **`3` (011)**: **MUL** — Умножить (`ACC` * `MUX B`).
*   **`4` (100)**: **DIV** — Разделить (`ACC` / `MUX B`).
*   **`5` (101)**: **MOD** — Остаток от деления (`ACC` % `MUX B`).
*   **`6` (110)**: **CMP** — Выполнить вычитание, но **не сохранять** ответ. Только обновить флаги `Z` и `N`.
*   **`7` (111)**: **PASS A** — Пропустить левый операнд (`ACC`) сквозь АЛУ без изменений. *(Используется для вывода в `OUT Port`)*.

### 3. `MUX A` [Биты 20:19] — Выбор адреса для памяти (DMEM)
Управляет мультиплексором перед адресным входом `DMEM`.
*   **`0` (00)**: **Inst Addr** — Прямой адрес из инструкции (команды типа `LD 5000` / `Direct`).
*   **`1` (01)**: **SP + Offs** — Адрес с аппаратного сумматора стека (локальные переменные `LD SP+2` / `Relative`).
*   **`2` (10)**: **Indirect (BR)** — Адрес из буферного регистра `BR`. Применяется на 2-м такте косвенной адресации (`LD [addr]`).
*   **`3` (11)**: **SP (Pure)** — Чистое значение указателя стека в обход сумматора. *(Используется для `PUSH`, `POP`, `CALL`, `RET`, чтобы адрес перехода в `arg` не исказил вершину стека)*.

### 4. `MUX B` [Биты 18:17] — Выбор правого операнда для АЛУ
Решает, откуда данные попадут на правый вход АЛУ для вычислений или загрузки.
*   **`0` (00)**: **Immediate** — Константа из защелки инструкции (например, число `5` в `ADD #5` или адрес прыжка `1024` в `JMP 1024`).
*   **`1` (01)**: **DMEM Data** — Данные, только что прочитанные из памяти по адресу, выбранному `MUX A`.
*   **`2` (10)**: **IN Port** — Токен, пришедший с устройства ввода (клавиатуры).
*   **`3` (11)**: *Зарезервировано.*

### 5. `MUX DATA` [Бит 16] — Выбор данных для записи в память
Управляет мультиплексором перед входом `DATA IN` памяти `DMEM`.
*   **`0`**: **ACC** — В память записывается текущее значение Аккумулятора (используется всегда: `ST`, `PUSH`).
*   **`1`**: **PC + 1 (Ret Addr)** — В память записывается адрес возврата от аппаратного инкрементора `ex_pc + 1` (используется **только** при команде `CALL`).

### 6. `Mem/Port Ctrl` [Биты 15:13] — Работа с внешними шинами
Управляет блоками памяти и Ввода/Вывода.
*   **`0` (000)**: **NONE** — Память спит.
*   **`1` (001)**: **MEM READ** — `DMEM` выдает данные на шину. *(Хитрость: если при этом Аккумулятор закрыт (`Reg En != 1`), данные автоматически защелкиваются во внутренний буфер `BR`)*.
*   **`2` (010)**: **MEM WRITE** — `DMEM` записывает данные с линии `DATA IN` по текущему адресу.
*   **`3` (011)**: **IN READ** — Сигнал для порта `IN` отдать токен.
*   **`4` (100)**: **OUT WRITE** — Сигнал (Write Enable) для порта `OUT` защелкнуть то, что сейчас находится на выходе АЛУ.

### 7. `Reg En` [Биты 12:10] — Дешифратор разрешения записи в регистры
Аппаратный дешифратор, определяющий, кто из регистров имеет право обновить свое значение в конце такта. Наличие кодов для комбинированных операций избавляет от необходимости усложнять микрокод.
*   **`0` (000)**: **NONE** — Все регистры закрыты (только чтение).
*   **`1` (001)**: **ACC WE** — Аккумулятор сохраняет новый результат с выхода АЛУ.
*   **`2` (010)**: **SP INC** — Выдает импульс на реверсивный счетчик регистра `SP` сделать `+1`.
*   **`3` (011)**: **SP DEC** — Выдает импульс на реверсивный счетчик регистра `SP` сделать `-1`.
*   **`4` (100)**: **PC WE** — Разрешение на перезапись счетчика команд (работает в связке с блоком `Cond` для прыжков).
*   **`5` (101)**: **PC WE + SP DEC** — Совмещенный сигнал: одновременно разрешает перезапись счетчика команд и уменьшает указатель стека. *(Используется исключительно на 2-м такте `CALL` для мгновенного завершения перехода)*.
*   **`6-7`**: *Зарезервировано.*

### 8. `Cond` [Биты 9:6] — Условия переходов (Branching)
Командует модулю `Condition Module`, нужно ли переключать `MUX PC` на адрес прыжка.
*   **`0` (0000)**: **JMP** — Безусловный переход. Модуль всегда выдает логическую `1`.
*   **`1` (0001)**: **JZ** — Выдает `1`, только если флаг `Z = 1` (последний результат был нулем).
*   **`2` (0010)**: **JNZ** — Выдает `1`, если флаг `Z = 0`.
*   **`3` (0011)**: **JLT** — Выдает `1`, если флаг `N = 1` (число было отрицательным).
*   **`4` (0100)**: **JGT** — Выдает `1`, если `Z = 0` И `N = 0` (строго больше нуля).
*   **`5..15`**: *Зарезервировано.*

### 9. `Next Addr` [Биты 5:0] — Адрес следующей микрокоманды
Определяет, куда `Micro-PC` перейдет на следующем такте внутри ПЗУ.
*   **`0` (000000)**: **FETCH** — Конец текущей макрокоманды! Конвейер берет следующую инструкцию, а `Address Mapper` сгенерирует новый стартовый адрес.
*   **`1..63`**: **Адрес в Micro-ROM** — Используется для продолжения сложной команды. Например, на микро-такте `44` (для `CALL`) здесь записано число `45`. На следующем такте автомат загрузит микрослово №45, чтобы завершить инструкцию.