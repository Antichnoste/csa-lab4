# 📖 Архитектурная шпаргалка (DataPath + Control Unit)

Данный документ описывает потактовое исполнение инструкций в 3-стадийном микропрограммном конвейере.

## 1. Универсальные стадии (Hardwired)
Первые два такта для **любой** инструкции работают аппаратно (без микрокода) и всегда занимают строго 1 такт каждый:

*   **Такт 1: Стадия IF (Выборка)**
    *   Аппаратный сумматор вычисляет `PC + 1`. Значение защелкивается обратно в `PC` (если нет сигналов `WE` или `STALL` от других стадий).
    *   Память `IMEM` читается по текущему адресу `PC`.
    *   Сырое 32-битное машинное слово защелкивается в `IF/ID Latch`.
*   **Такт 2: Стадия ID (Декодирование)**
    *   Дешифратор (*Instruction Decoder*) расплетает 32-битное слово на шины `Opcode`, `Mode` и `Arg`.
    *   Текущие значения архитектурных регистров (`SP`, `ACC`, `PC`) пробрасываются транзитом.
    *   Всё это состояние фиксируется в `ID/EX Latch`.
    *   `Opcode` и `Mode` отправляются в `Address Mapper` (блок CU), который мгновенно генерирует стартовый адрес для регистра `Micro-PC`.

---

## 2. Исполнение: Режимы адресации (Стадия EX)
Выбор источника данных осуществляется мультиплексорами `MUX A` (адрес памяти) и `MUX B` (правый операнд АЛУ).

*   **`Immediate` (Константа):** Память `DMEM` не используется. `MUX Src B = 0` пропускает константу из защелки `ID/EX` напрямую в АЛУ.
*   **`Direct` (Прямая):** `MUX Addr = 0` пропускает `Inst Addr` в память. Прочитанные данные через `MUX Src B = 1` идут в АЛУ. (1 такт)
*   **`Relative` (Локальная / SP+Offset):** Аппаратный `ADDR ADDER` складывает `SP` и `Offset`. `MUX Addr = 1` подает эту сумму в память. Данные через `MUX Src B = 1` идут в АЛУ. (1 такт)
*   **`Indirect` (Косвенная):** Требует **2 такта** и сигнала `STALL`.
    *   *Микро-такт 1:* `MUX Addr = 0` (Inst Addr). Читаем память. Аккумулятор закрыт (`Reg En=0`), поэтому прочитанный указатель аппаратно защелкивается во временный регистр **`BR`**.
    *   *Микро-такт 2:* `MUX Addr = 2` (Indirect). Адрес из `BR` подается в память. Прочитанные финальные данные через `MUX Src B = 1` идут в АЛУ.

---

## 3. Потактовый разбор инструкций (Стадия EX)

### Группа А: Арифметика и Логика (`ADD`, `SUB`, `MUL`, `DIV`, `MOD`, `CMP`)
*Занимают 1 такт в EX (если не Indirect).*
*   **Сигналы CU:** В зависимости от `Mode` настраиваются `MUX A` и `MUX B`. Выдается `ALU Op = 1..6`. Разрешается запись в аккумулятор: `Reg En = 1` (ACC WE).
*   **DataPath:** Левый операнд (ACC) и правый операнд (MUX B) попадают в АЛУ. Вычисляется результат и флаги `Z`, `N`. Результат по обратной петле защелкивается в `ACC`.
*   *(Для `CMP` `Reg En = 0`, результат отбрасывается, сохраняются только флаги).*

### Группа Б: Работа с памятью (`LD`, `ST`)
*   **`LD` (Загрузка в ACC):** Аналогично арифметике, но `ALU Op = 0` (PASS B). Данные из `MUX B` просто проходят сквозь АЛУ без изменений и защелкиваются в `ACC`.
*   **`ST` (Запись в память):**
    *   **Сигналы CU:** `Mem/Port = 2` (DMEM WE). `MUX DATA = 0` (пропустить ACC). АЛУ отдыхает.
    *   **DataPath:** Аккумулятор подается на линию `DATA IN` памяти. По указанному `MUX A` адресу происходит аппаратная запись.

### Группа В: Ввод-Вывод (`IN`, `OUT`)
*   **`IN` (Чтение порта):** `Mem/Port = 3` (IN READ). `MUX B = 2` переключается на IN Port. АЛУ делает `PASS B`. Токен защелкивается в `ACC`.
*   **`OUT` (Запись в порт):** `ALU Op = 7` (PASS A). Аккумулятор проходит сквозь АЛУ. `Mem/Port = 4` (OUT WE) заставляет порт `OUT` защелкнуть данные с выхода АЛУ.

### Группа Г: Ветвления (`JMP`, `JZ`, `JNZ`, `JLT`, `JGT`)
*Занимают 1 такт в EX. Генерируют FLUSH при успешном прыжке.*
*   **Сигналы CU:** `MUX B = 0` (Immediate - адрес прыжка). `ALU Op = 0` (PASS B). `Cond = 0..4`.
*   **DataPath:** Адрес прыжка пролетает сквозь АЛУ и по длинной шине идет к `MUX PC`. Модуль условий `Condition Module` сверяет код `Cond` с флагами АЛУ.
*   **Если прыжок успешен:** Выдается сигнал `Branch Ctrl` (переключает `MUX PC` на шину АЛУ) и `Reg En = 4` (PC WE). Адрес записывается в `PC`. Одновременно генерируется `Latch Ctrl = 2` (FLUSH), стирая из защелок мусор, прочитанный по старым адресам.

### Группа Д: Работа со стеком (`PUSH`, `POP`)
*Требуют 2 такта в EX.*
*   **`PUSH` (Запись на стек):**
    *   *Микро-такт 1:* `Latch Ctrl = 1` (**STALL**). `MUX A = 3` (Pure SP). `MUX DATA = 0` (ACC). `Mem/Port = 2` (DMEM WE). Значение ACC пишется в память по адресу `SP`.
    *   *Микро-такт 2:* `Reg En = 3` (**SP DEC**). Счетчик `SP` аппаратно уменьшается на 1. `STALL` снимается.
*   **`POP` (Чтение со стека):**
    *   *Микро-такт 1:* `Latch Ctrl = 1` (**STALL**). `Reg En = 2` (**SP INC**). Счетчик `SP` увеличивается на 1 (сдвигается на занятую ячейку).
    *   *Микро-такт 2:* `MUX A = 3` (Pure SP). `Mem/Port = 1` (READ). `MUX B = 1` (DMEM). `ALU Op = 0` (PASS B). `Reg En = 1` (ACC WE). Данные из памяти проходят сквозь АЛУ в `ACC`.

### Группа Е: Подпрограммы (`CALL`, `RET`)
*Высший пилотаж конвейерной маршрутизации.*
*   **`CALL` (Вызов):**
    *   *Микро-такт 1:* `Latch Ctrl = 1` (**STALL**). `MUX A = 3` (Pure SP). `MUX DATA = 1` (**PC+1**). `Mem/Port = 2` (WRITE). Адрес возврата (проброшенный через защелку и сложенный с `+1`) записывается на вершину стека!
    *   *Микро-такт 2:* `MUX B = 0` (Immediate). `ALU Op = 0` (PASS B). Выдается совмещенный код `Reg En = 5` (**PC WE + SP DEC**). `Latch Ctrl = 2` (**FLUSH**). За один такт `SP` уменьшается, адрес прыжка пролетает через АЛУ в `PC`, а конвейер очищается.
*   **`RET` (Возврат):**
    *   *Микро-такт 1:* `Latch Ctrl = 1` (**STALL**). `Reg En = 2` (**SP INC**).
    *   *Микро-такт 2:* `MUX A = 3` (Pure SP). `MUX B = 1` (DMEM). `ALU Op = 0` (PASS B). Прочитанный из стека адрес проходит через АЛУ в `MUX PC`. `Reg En = 4` (**PC WE**). `Latch Ctrl = 2` (**FLUSH**). Прыжок совершен.

***

# 💡 Главные тезисы для ответов преподавателю

1.  **Почему микрокод только на стадии EX?**
    *"Потому что стадии IF и ID выполняют тривиальные, всегда одинаковые операции (инкремент PC и сплиттинг инструкции). Применять к ним микрокод — значит излишне раздувать Micro-ROM. Микрокод активируется только там, где требуется сложное многотактовое управление ресурсами (АЛУ, Память, MUX)."*
2.  **Как решается конфликт конвейера при 2-тактовых операциях?**
    *"С помощью аппаратного сигнала `STALL`. Если микропрограмма состоит из >1 шага, поле `Latch Ctrl` выдает `STALL`. Это замораживает защелки `IF/ID` и `ID/EX`, блокируя чтение новых инструкций, пока `Micro-PC` не завершит микропрограмму (не дойдет до адреса `0`)."*
3.  **Как решается "штраф ветвления" (Branch Penalty)?**
    *"Позднее разрешение ветвления на стадии EX требует очистки конвейера. Если модуль условий одобряет переход, аппаратура переключает `MUX PC`, защелкивает новый адрес и генерирует аппаратный `FLUSH`, который превращает инструкции в стадиях IF и ID в `NOP`."*
4.  **Как АЛУ задействовано в переходах?**
    *"Для экономии внутренних шин данных все целевые адреса для прыжков (`JMP`, `CALL`, `RET`) маршрутизируются транзитом через АЛУ с использованием микрооперации `PASS B`. Выход АЛУ соединен с `MUX PC` шиной `ALU Out / Branch Target`."*
5.  **Зачем нужен регистр BR?**
    *"Для предотвращения комбинационной петли при косвенной адресации. На первом такте мы читаем указатель и защелкиваем его в `BR`. На втором такте этот стабильный указатель подается на `MUX Addr` для извлечения целевых данных."*